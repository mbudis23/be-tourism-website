const express = require('express');
const router = express.Router();
const ratingController = require('../controllers/ratingController');

// router.get('//insertRatings', ratingController.insertTourismRatings);

module.exports = router;
